[package]
name = "optigrade-optimizer"
version = "0.1.0"
edition = "2021"
description = "Parallel optimization core for OptiGrade."

[dependencies]
rand = "0.8"
rayon = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
pretty_assertions = "1.4"

